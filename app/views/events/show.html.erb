<div class="container">
      <h3>Event</h3>

      <h4><%= @event.name %></h4>

      <%= link_to "Edit the description", edit_event_path(@event), class: "btn btn-info" %>

      <%= link_to "home", root_path  %>

      <%= link_to "Delete", event_path(@event), method: :delete %>
</div>
